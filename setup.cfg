[metadata]
name = solis-service
version = 1.1.0

[options]
packages = solis_service
install_requires = pint
    influxdb-client[ciso]
python_requires = >=3.7

[options.entry_points]
console_scripts =
    solis_service = solis_service.server:run

